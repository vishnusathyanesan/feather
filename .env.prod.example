# =============================================================================
# Feather Production Environment Variables
# Copy to .env.prod and fill in all values before deploying
# =============================================================================

# Domain — your DuckDNS subdomain (e.g., feather-chat.duckdns.org)
DOMAIN=your-app.duckdns.org

# DuckDNS token (from https://www.duckdns.org after logging in)
DUCKDNS_TOKEN=your-duckdns-token

# Database
DB_USER=feather
DB_PASSWORD=change-me-strong-password
DB_NAME=feather

# Redis
REDIS_PASSWORD=change-me-redis-password

# JWT — generate with: openssl rand -base64 32
JWT_SECRET=change-me-jwt-secret

# MinIO (S3-compatible file storage)
MINIO_ACCESS_KEY=feather-minio-admin
MINIO_SECRET_KEY=change-me-minio-secret

# Google OAuth (optional — leave empty to disable)
GOOGLE_CLIENT_ID=
